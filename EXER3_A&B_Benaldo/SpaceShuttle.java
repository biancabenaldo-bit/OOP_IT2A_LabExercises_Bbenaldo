public class SpaceShuttle extends AirTransport {
    public SpaceShuttle(String fuelType, int passengers, double speed, int altitude) {
        super(fuelType, passengers, speed, altitude);
    }
}